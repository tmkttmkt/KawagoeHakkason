const express = require('express');
const multer = require('multer');
const path = require('path');

const router = express.Router();

router.post('/uploadee', (req, res) => {
    console.log("suygge")
});

module.exports = router;